# ==========================================
# Computer Vision & Deep Learning Models
# ==========================================
ultralytics>=8.1.0        # สำหรับ YOLOv8 (Tracking) และ YOLO11 (Pose)
tensorflow>=2.14.0        # สำหรับสร้างและเทรนโมเดล BiLSTM / BiGRU
# หมายเหตุ: หากใช้ GPU ควรใช้ tensorflow[and-cuda] (สำหรับ Linux/WSL) 
# หรือติดตั้ง CUDA Toolkit แยกต่างหากสำหรับ Windows

# ==========================================
# Data Manipulation & Array Processing
# ==========================================
numpy>=1.24.0             # จัดการ Array และฟีเจอร์
pandas>=2.0.0             # จัดการไฟล์ CSV และ DataFrame
scipy>=1.10.0             # สำหรับการทำ Interpolation (แทรกเฟรมที่หายไป) ใน Step 4

# ==========================================
# Image Processing & Video 
# ==========================================
opencv-python>=4.8.0      # สำหรับอ่าน/เขียนวิดีโอ (cv2)

# ==========================================
# Machine Learning Utilities & Metrics
# ==========================================
scikit-learn>=1.3.0       # สำหรับ Train/Test Split, การทำ Scaling และวัดผล (ROC, AUC, F1)

# ==========================================
# Visualization & Reporting
# ==========================================
matplotlib>=3.7.0         # พล็อตประวัติการเทรนโมเดล และ ROC Curve
seaborn>=0.12.0           # พล็อต Confusion Matrix ให้สวยงาม

# ==========================================
# Utilities
# ==========================================
tqdm>=4.65.0              # สร้างแถบ Progress bar สวยๆ ตอนรันลูป
psutil>=5.9.0             # สำหรับเช็กสถานะ RAM (ใช้ใน Step 2)